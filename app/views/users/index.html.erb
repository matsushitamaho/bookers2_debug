<div>
  <table>
    <th>image</th>
    <th>name</th>
  </table>
  <tbody>
    <tr>
      <td><%= image_tag @user.get_profile_image(50,50) %></td>
      <td><%= @user.name %></td>
      <td><%= link_to "Show", user_path(user.id) %></td>
    </tr>
  </tbody>
</div>